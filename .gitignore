node_modules
dist
dist-ssr
*.local
.DS_Store
.idea
*.tsbuildinfo

# public/cv-en.pdf and public/cv-de.pdf are committed on purpose — see README.
# The Cloudflare Workers build environment blocks Puppeteer's Chromium
# download (postinstall scripts are gated), so the PDFs can't be generated
# during deploy; they have to be pre-built locally and checked in instead.
